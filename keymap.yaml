layout:
  ortho_layout:
    split: false
    rows: 4
    columns: 12  # ここを cols から columns に変更しました
layers:
  Base:
  # 1行目
  - [Q, W, E, R, T, '', '', Y, U, I, O, P]
  # 2行目
  - [A, S, D, F, G, '', '', H, J, K, L, ';']
  # 3行目
  - [Z, X, C, V, B, '', '', N, M, ',', '.', /]
  # 4行目
  - [
      ESC, Gui, Alt, {t: '○', type: ghost}, Enter, Tab,
      Bksp, Space, {t: '●', type: ghost}, ']', '\', /
    ]
